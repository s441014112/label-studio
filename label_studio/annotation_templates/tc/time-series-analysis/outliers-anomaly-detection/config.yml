title: "異常值檢測"
type: community
group: 時間序列分析
image: /static/templates/outliers-anomaly-detection.png
details: |
  <h1>標記時間序列資料中的異常點</h1>
  <dl>
    <dt>產業應用</dt>
    <dd>品質保證、詐欺檢測、設備監控、網路監控、感測器異常</dd>
    <dt>相關模型</dt>
    <dd>Isolation Forest、LOF、AutoEncoder、統計方法</dd>
    <dt>領域術語</dt>
    <dd>異常檢測、離群值、時間序列異常、異常標註</dd>
  </dl>
config: |
  <View>
      <!-- Object tag for time series data source -->
      <TimeSeries name="ts" valueType="url" value="$csv"
                  sep=","
                  timeColumn="time"
                  timeFormat="%Y-%m-%d %H:%M:%S.%f"
                  timeDisplayFormat="%Y-%m-%d"
                  overviewChannels="velocity">

          <Channel column="velocity"
                   units="miles/h"
                   displayFormat=",.1f"
                   strokeColor="#1f77b4"
                   legend="Velocity"/>
      </TimeSeries>

      <!-- Control tag for region labels -->
      <TimeSeriesLabels name="label" toName="ts">
          <Label value="Region" background="red" />
      </TimeSeriesLabels>

      <Choices name="region_type" toName="ts"
            perRegion="true" required="true">
          <Choice value="Outlier"/>
          <Choice value="Anomaly"/>
      </Choices>
  </View>

