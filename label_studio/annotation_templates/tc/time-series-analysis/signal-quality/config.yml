title: 訊號品質
type: community
group: 時間序列分析
image: /static/templates/signal-quality.png
details: |
  <h1>評估和標註訊號品質</h1>
  <dl>
    <dt>產業應用</dt>
    <dd>工業設備、醫療設備監測、電信、音訊訊號處理、振動分析、環境監測、電力系統、汽車診斷、航空航天、製造品質控制、預測性維護、物聯網感測器監測</dd>
    <dt>相關模型</dt>
    <dd>信噪比、資料品質評估、感測器驗證、訊號完整性</dd>
    <dt>領域術語</dt>
    <dd>CSV、Excel、XLS、Sheet、訊號退化、雜訊檢測、資料可靠性、測量品質、感測器校準</dd>
  </dl>
config: |
  <View>
      <!-- No region selected section -->
      <View visibleWhen="no-region-selected"
            style="height:120px">

          <!-- Control tag for region labels -->
          <TimeSeriesLabels name="label" toName="ts">
              <Label value="Region" background="#5b5"/>
          </TimeSeriesLabels>
      </View>

      <!-- Region selected section with choices and rating -->
      <View visibleWhen="region-selected" style="height:120px">

          <!-- Per region Rating -->
          <Rating name="rating" toName="ts"
                  maxRating="10" icon="star"
                  perRegion="true"/>
          <!-- Per region Choices  -->
          <Choices name="choices" toName="ts"
                   showInline="true" required="true"
                   perRegion="true">
              <Choice value="Good"/>
              <Choice value="Medium"/>
              <Choice value="Poor"/>
          </Choices>
      </View>

      <!-- Object tag for time series data source -->
      <TimeSeries name="ts" valueType="url" value="$csv"
                  sep="," timeColumn="time">
          <Channel column="signal_1"
                   strokeColor="#17b" legend="Signal 1"/>
          <Channel column="signal_2"
                   strokeColor="#f70" legend="Signal 2"/>
      </TimeSeries>
  </View>

